name: CI – Android

on:
  push:
    branches: [main, master, develop]
    paths:
      - 'src/**'
      - 'android/**'
      - 'Makefiles/Android.mk'
      - 'Makefiles/Application.mk'
      - '.github/workflows/ci_android.yml'
  pull_request:
    branches: [main, master, develop]

jobs:
  build:
    name: Build Android APK (minSdk 19 / targetSdk 36)
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK and platform
        run: |
          echo "y" | sdkmanager --install \
            "ndk;26.3.11579264" \
            "build-tools;34.0.0" \
            "platforms;android-36"

      - name: Cache SDL2 Android sources
        id: cache-sdl2-android
        uses: actions/cache@v4
        with:
          path: android/app/src/main/jni
          key: sdl2-android-2.30.10-v2

      - name: Create jni directories
        run: |
          mkdir -p android/app/src/main/jni
          mkdir -p android/app/src/main/c/audio
          mkdir -p android/app/src/main/c/fonts

      - name: Download SDL2 Android sources
        if: steps.cache-sdl2-android.outputs.cache-hit != 'true'
        run: |
          SDL_VER="2.30.10"
          MIX_VER="2.8.0"
          TTF_VER="2.22.0"
          JNI="android/app/src/main/jni"

          curl -sL "https://github.com/libsdl-org/SDL/releases/download/release-$SDL_VER/SDL2-$SDL_VER.tar.gz" \
            | tar -xz --strip-components=1 -C "$JNI" --one-top-level=SDL2
          curl -sL "https://github.com/libsdl-org/SDL_mixer/releases/download/release-$MIX_VER/SDL2_mixer-$MIX_VER.tar.gz" \
            | tar -xz --strip-components=1 -C "$JNI" --one-top-level=SDL2_mixer
          curl -sL "https://github.com/libsdl-org/SDL_ttf/releases/download/release-$TTF_VER/SDL2_ttf-$TTF_VER.tar.gz" \
            | tar -xz --strip-components=1 -C "$JNI" --one-top-level=SDL2_ttf

      - name: Copy game source files into jni tree
        run: |
          cp src/*.c   android/app/src/main/c/ 2>/dev/null || true
          cp src/*.h   android/app/src/main/c/ 2>/dev/null || true
          cp src/audio/*.c android/app/src/main/c/audio/ 2>/dev/null || true
          cp src/audio/*.h android/app/src/main/c/audio/ 2>/dev/null || true
          cp src/fonts/*.c android/app/src/main/c/fonts/ 2>/dev/null || true
          cp src/fonts/*.h android/app/src/main/c/fonts/ 2>/dev/null || true
          cp android/app/src/main/jni/Android.mk android/app/src/main/jni/Android.mk || true
          cp android/app/src/main/jni/Application.mk android/app/src/main/jni/Application.mk || true

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        run: |
          cd android
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Verify APK
        run: find android -name "*.apk" | xargs ls -lh

      - uses: actions/upload-artifact@v4
        with:
          name: blockamokremix-android-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14

  lint:
    name: Lint Android Manifest & Gradle
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4

      - name: Check screenOrientation is landscape
        run: |
          grep -q 'screenOrientation="landscape"' android/app/src/main/AndroidManifest.xml \
            && echo "✅ landscape orientation confirmed" \
            || (echo "❌ screenOrientation is not landscape"; exit 1)

      - name: Check targetSdk is 36
        run: |
          grep -q 'targetSdk 36' android/app/build.gradle \
            && echo "✅ targetSdk 36" \
            || (echo "❌ targetSdk != 36"; exit 1)

      - name: Check minSdk is 19
        run: |
          grep -q 'minSdk 19' android/app/build.gradle \
            && echo "✅ minSdk 19" \
            || (echo "❌ minSdk != 19"; exit 1)

      - name: Check compileSdk is 36
        run: |
          grep -q 'compileSdk 36' android/app/build.gradle \
            && echo "✅ compileSdk 36" \
            || (echo "❌ compileSdk != 36"; exit 1)
